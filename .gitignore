# Working spec (kept out of the repo)
/prompt.md

# Raw source material (CV PDFs + full-res photos, ~150MB — optimized copies live in /images)
/new content/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp
*.swo

# Dependencies / build (if added later)
node_modules/
dist/
build/
